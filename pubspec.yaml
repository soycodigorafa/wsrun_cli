name: wsrun_cli
description: Run VS Code workspace launch configurations from any terminal. Flutter-first TUI.
version: 0.3.1
homepage: https://github.com/soycodigorafa/wsrun_cli

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.4.0
  path: ^1.9.0
  process_run: ^0.14.0
  nocterm: ^0.6.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0

executables:
  wsrun: wsrun_cli
